import {
  BookOpen,
  Clapperboard,
  GraduationCap,
  UserRound,
} from "lucide-react";

export const authorNavIcons = {
  blog: BookOpen,
  courses: GraduationCap,
  videos: Clapperboard,
  about: UserRound,
} as const;
